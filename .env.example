# TRACEID — Environment Variables
# Copy this file to .env and fill in values. NEVER commit .env itself.
# All keys are documented in docs/CONFIGURATION.md.

# =============================================================================
# DATABASE
# =============================================================================
DATABASE_URL=postgresql://traceid:traceid@localhost:5432/traceid

# =============================================================================
# API SERVER
# =============================================================================
API_HOST=0.0.0.0
API_PORT=8000
DEBUG=false
LOG_LEVEL=INFO

# =============================================================================
# DEMO MODE
# =============================================================================
# Set to true for offline demo with replay LLM and sandbox corpus only
DEMO_MODE=false

# =============================================================================
# SOURCE ADAPTERS
# =============================================================================
# JSON array of approved domain patterns
ALLOWED_DOMAINS=["*.example"]
SANDBOX_CORPUS_PATH=backend/seed/sandbox_corpus
LIVE_ADAPTER_ENABLED=false
ADAPTER_TIMEOUT_SECONDS=30
ADAPTER_RATE_LIMIT_PER_DOMAIN=5

# =============================================================================
# SANITIZER
# =============================================================================
SANITIZE_MAX_LENGTH=50000

# =============================================================================
# INDEPENDENCE ENGINE
# =============================================================================
INDEPENDENCE_SIMILARITY_THRESHOLD=0.85
INDEPENDENCE_SHINGLE_SIZE=5
INDEPENDENCE_MERGE_ON_UNCERTAINTY=true

# =============================================================================
# RESOLUTION ENGINE
# =============================================================================
RESOLUTION_NAME_SIMILARITY_THRESHOLD=0.85
RESOLUTION_USERNAME_SIMILARITY_THRESHOLD=0.90
RESOLUTION_EMBEDDING_ENABLED=false
RESOLUTION_EMBEDDING_WEIGHT=0.0

# =============================================================================
# STATUS ENGINE (decision rules v1)
# =============================================================================
STATUS_MIN_CLUSTERS_STRONG=2
STATUS_MIN_DISCRIMINATING_STRONG=1
STATUS_MARGIN_THRESHOLD=1
STATUS_MIN_CLUSTERS_POSSIBLE=1

# =============================================================================
# PIPELINE
# =============================================================================
MAX_ITERATIONS=3
PIPELINE_TIMEOUT_SECONDS=300

# =============================================================================
# LLM PROVIDER
# =============================================================================
# Providers: mock, replay, gemini, openai
LLM_PROVIDER=mock
# Fill in your API key when using a real provider. NEVER commit this value.
LLM_API_KEY=
LLM_MODEL=gemini-2.5-flash
LLM_TEMPERATURE=0.1
LLM_TIMEOUT_SECONDS=60
LLM_MAX_RETRIES=2
LLM_REPLAY_DIR=backend/seed/llm_replays
LLM_RECORD_MODE=false

# =============================================================================
# IMAGE HANDLING (D2)
# =============================================================================
IMAGE_MAX_SIZE_BYTES=10485760
IMAGE_STRIP_EXIF=true
FACE_SIMILARITY_ENABLED=false
FACE_SIMILARITY_WEIGHT=0.0

# =============================================================================
# OCR
# =============================================================================
OCR_ENABLED=true
# Engine: tesseract or fixture
OCR_ENGINE=tesseract
OCR_FIXTURE_PATH=backend/seed/ocr_fixtures

# =============================================================================
# RATE LIMITING
# =============================================================================
RATE_LIMIT_CASE_CREATE=10/minute
RATE_LIMIT_COPILOT=30/minute

# =============================================================================
# RETENTION
# =============================================================================
DATA_RETENTION_DAYS=30
AUTO_DELETE_EXPIRED=false

# =============================================================================
# FRONTEND
# =============================================================================
VITE_API_BASE_URL=http://localhost:8000
